# Day 1：文本如何变成 Transformer 输入

> 本文件由教程脚本自动生成。请修改源脚本后重新渲染，不要手工编辑。

生成来源：`lessons/transformer/day01-input-representation/run.sh`

## 1. 当前位于完整推理的哪一步

```text
我喜欢打
-> [本课] Token ID -> 初始向量
-> [Day 2] Self-Attention 读取上下文
-> 多层 Transformer
-> 预测下一个 Token：网
```
本课只回答：文本 `我喜欢打` 怎样变成 Transformer 可以接收的向量序列？
这是推理的输入准备阶段，不涉及训练、Loss 或参数更新。

## 2. 本课的输入与输出

**输入：** 一段文本 `我喜欢打`

**输出：** 形状为 `(3, 4)` 的向量矩阵

这 3 行分别对应 `我`、`喜欢`、`打`，每行 4 个数。
这些向量将直接交给下一步 Self-Attention。

## 3. 文本先被切成 Token

Tokenizer 的结果来自真实运行：

| 位置 | Token | Token ID |
| ---: | --- | ---: |
| 0 | `我` | `7522` |
| 1 | `喜欢` | `69681` |
| 2 | `打` | `15552` |

因此：
```text
文本：我喜欢打
Token：['我', '喜欢', '打']
Token ID：[7522, 69681, 15552]
```
Token ID 是词表中的行号，不是语义分数，也不是 Embedding 向量。

## 4. Token ID 查询 Token Embedding

Embedding 层可以看成一张可训练的大表：
```text
Token ID 7522  -> 查第 7522 行 -> 得到“我”的初始向量
Token ID 69681 -> 查第 69681 行 -> 得到“喜欢”的初始向量
Token ID 15552 -> 查第 15552 行 -> 得到“打”的初始向量
```
本实验把每个向量缩短为 4 个数，便于阅读。真实模型通常有更多维度。

| Token | Token ID | 查表得到的 4 维向量 |
| --- | ---: | --- |
| `我` | `7522` | `[-0.580, 0.309, 0.484, -0.923]` |
| `喜欢` | `69681` | `[-0.108, 1.070, 1.137, 1.043]` |
| `打` | `15552` | `[-0.312, -0.273, -0.103, 0.752]` |

当前数值由固定随机种子生成，只用于演示查表和形状，不代表真实语义。

## 5. 再加入位置信息

Token Embedding 只表达 Token 身份。位置向量补充它位于第 0、1、2 个位置。

以最后一个 Token `打` 为例：
```text
Token 向量：[-0.312 -0.273 -0.103  0.752]
位置向量： [ 1.762 -2.234  0.164  0.75 ]
相加结果： [ 1.45  -2.507  0.061  1.502]
```
相加后的结果才是 `打` 进入第一个 Transformer 层的输入向量。

## 6. 最终交给 Transformer 的内容

三行向量上下排列成一个矩阵：
```text
[[-0.557  1.964  0.766 -0.978]
 [-1.371  0.545  0.113  0.524]
 [ 1.45  -2.507  0.061  1.502]]
```
Shape 是 `(3, 4)`：3 行对应 3 个 Token，4 列对应每个向量的 4 个分量。

> 本课的输出不是预测结果，只是下一步 Self-Attention 的输入。

## 7. 本课结论

```text
我喜欢打
-> [我] [喜欢] [打]
-> [7522, 69681, 15552]
-> 查询 Token Embedding
-> 加入位置信息
-> 3 个初始向量
```
下一课将只跟踪最后一个 Token `打`，观察它如何读取整个提示词的上下文。
