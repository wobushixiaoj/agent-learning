# Day 1：文本如何变成 Transformer 输入：脚本输出教材

> 本文件由教程脚本自动生成。请修改源脚本后重新渲染，不要手工编辑。

生成来源：`lessons/transformer/day01-input-representation/run.sh`

```text
========================================================================
Day 1：文本如何变成 Transformer 可以处理的输入
========================================================================
本节只学习输入表示，不涉及 Attention、预测目标或训练过程。

========================================================================
步骤 1：Text -> Token -> Token ID
========================================================================
原始文本：
Agent uses tools to complete a task.

Tokenizer 切分出的 Token：
['Agent', ' uses', ' tools', ' to', ' complete', ' a', ' task', '.']

每个 Token 在词表中的编号（Token ID）：
[36772, 3544, 4899, 284, 1844, 257, 4876, 13]

Token 数量：8

本步结论：Token ID 只是词表索引，还不是语义向量。

========================================================================
步骤 2：Token ID -> Embedding -> Transformer 输入
========================================================================
Embedding 层是一张可训练的向量表。
Token ID 用来选择表中的某一行。

Token ID 张量形状：          (8,)
Token Embedding 形状：       (8, 8)
Position Embedding 形状：    (8, 8)
最终 Transformer 输入形状：  (8, 8)

以第一个 Token 为例：
Token：          'Agent'
Token ID：       36772
Token 向量：     [-0.227 -0.508  0.59   1.453 -1.225  0.465  0.284  0.77 ]
位置向量：       [ 2.285 -1.479 -0.756 -0.721  1.631 -1.067  0.259  0.697]
两者相加的结果： [ 2.058 -1.986 -0.166  0.731  0.406 -0.602  0.543  1.468]

验证同一个 Token ID 是否查到相同向量：
两个 Token ID：[36772, 36772]
查到的 Token 向量是否完全相同：是
本步结论：Token Embedding 表达 Token 是什么；Position Embedding 提供它在序列中的位置信息。

========================================================================
Day 1 完整链路
========================================================================
文本 -> Token -> Token ID -> Token 向量 + 位置向量 -> Transformer 输入

下一步 Day 2：让每个 Token 通过 Self-Attention 读取上下文信息。
```
