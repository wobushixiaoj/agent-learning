# Day 2：Self-Attention 如何读取上下文

## 当前位于哪里

Day 1 已经让每个 Token 拥有自己的输入向量。本节只研究这些向量如何互相读取：

```text
输入向量 X
-> Q、K、V
-> Token 两两匹配分数
-> Causal Mask
-> Softmax 权重
-> 对 V 加权求和
-> 上下文化向量
```

## 先补一个前置：矩阵形状

本节有 3 个 Token，每个 Token 使用 4 维向量：

```text
X.shape = (Token 数量, 每个 Token 的向量维度) = (3, 4)
```

| 行 | Token | 四维向量 |
| ---: | --- | --- |
| 1 | `Agent` | `[1, 0, 1, 0]` |
| 2 | `uses` | `[0, 1, 1, 0]` |
| 3 | `tools` | `[1, 1, 0, 1]` |

所以 `(3, 4)` 是下面矩阵的尺寸说明，不是另一组计算结果。矩阵共有
`3 x 4 = 12` 个数字。

## 本节只解决什么

```text
一个 Token 如何决定从其他 Token 读取多少信息？
```

为了隔离这一个问题，教程暂时设置 `Q = K = V = X`。真实模型使用三组可训练
矩阵生成 Q、K、V，后续课程再展开。

## 运行

从仓库根目录执行：

```bash
./lessons/transformer/day02-self-attention/run.sh
```

第一次只跟随中文输出，不要求先阅读源码。
