# Agent Learning 仓库协作规则

## 仓库目标

本仓库用于面向高级 Agent 应用或平台工程师的长期学习。代码实验不是孤立
Demo，而是教材的一部分；学习者在对话中提出的问题是教材缺失信息的证据。

## 教学顺序

每次开始一个实验前，必须先用中文交代：

1. 当前内容位于完整知识链的什么位置；
2. 已经掌握的输入是什么；
3. 本节只解决哪一个问题；
4. 本节明确不讨论什么；
5. 运行哪条命令、重点观察哪段输出。

不得在没有说明上下文的情况下，从架构输入跳到训练过程、损失函数或面试题。

## 用户提问即教材断点

学习者提出的每个技术问题、反问、困惑或纠正，都必须执行以下动作：

1. 判断问题属于哪一条知识链；
2. 更新对应课程 README 或教程脚本，使下一位学习者不必再问同一个前置问题；
3. 在 `docs/learning-breakpoints.md` 记录原问题、触发位置、缺失链路和教材修改；
4. 如果问题来自教学上下文缺失，标记为 `context-induced`，不得归因成学习者错误；
5. 如果是概念错误，保留原理解、修正链路和一道迁移自测题；
6. 更新与问题相邻的上下游链接，保持知识图谱可追踪。

不能只在对话里回答问题而不修改教材。

## 教材与脚本规范

- 默认使用中文，必要的英文术语放在中文解释后，例如“查询向量（Query）”。
- 终端脚本必须像教程：先讲目的，再讲输入，再讲计算，最后解释输出。
- 打印矩阵时必须说明形状、行含义、列含义，并至少展开一个具体行。
- 首次实验优先使用最小数字和可手算例子，不提前引入无关复杂度。
- 每节只解决一个主要认知跳转，其他概念放入后续课程。
- 教材必须区分稳定原理、为教学做的简化和真实模型中的实现。
- 用户尚未运行实验前，不直接进入结果追问。

## 验证要求

修改 Python 教材脚本后至少运行：

```bash
python3 -m py_compile <modified-files>
./<modified-run-script>
```

检查输出是否为中文、是否包含课程位置和本节结论，并确认 README 中的命令可从
仓库根目录直接执行。

## 文件所有权

- `README.md`：仓库入口和当前学习方式；
- `docs/learning-roadmap.md`：整体课程顺序与边界；
- `docs/learning-breakpoints.md`：真实提问驱动的教材变更记录；
- `lessons/**/README.md`：单节教材；
- `lessons/**/lesson.py`：可直接运行的中文教程脚本；
- `lessons/**/lab.py`：可选的逐段实验文件。
