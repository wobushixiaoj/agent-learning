# Agent Learning 仓库协作规则

## 仓库目标

本仓库用于面向高级 Agent 应用或平台工程师的长期学习。代码实验不是孤立
Demo，而是教材的一部分；学习中的问题只用于发现教材缺失，不作为课程内容保留。

## 教学顺序

Transformer 基础课程统一使用 `我喜欢打 -> 网 -> 球` 作为贯穿案例。先完成推理主线，
再单独进入训练主线；不得在同一条初学主线中来回切换训练和推理视角。

每次开始一个实验前，必须先用中文交代：

1. 当前内容位于完整知识链的什么位置；
2. 已经掌握的输入是什么；
3. 本节只解决哪一个问题；
4. 本节明确不讨论什么；
5. 运行哪条命令、重点观察哪段输出。

不得在没有说明上下文的情况下，从架构输入跳到训练过程、损失函数或面试题。

每节正文必须先沿一个具体 Token 走通“输入 -> 中间变化 -> 输出”，再补充批量矩阵
写法、统计推导和工程优化。任何组件都必须说明它接收什么、输出什么、输出交给谁；
不得让 Attention 看起来像是直接输出下一个 Token。

## 问题驱动教材改进

学习者提出的每个技术问题、反问、困惑或纠正，都必须执行以下动作：

1. 判断问题属于哪一条知识链；
2. 更新对应课程 README 或教程脚本，使下一位学习者不必再问同一个前置问题；
3. 只把经过整理的知识结论、必要推导和迁移练习写入课程；
4. 不在仓库中记录学习者原话、错误回答、个人理解轨迹或学习画像；
5. 更新与问题相邻的上下游链接，保持知识图谱可追踪。

不能只在对话里回答问题而不修改教材。个人复盘过程由仓库外的 Obsidian 知识卡片维护。

## 教材与脚本规范

- 默认使用中文，必要的英文术语放在中文解释后，例如“查询向量（Query）”。
- 终端脚本必须像教程：先讲目的，再讲输入，再讲计算，最后解释输出。
- 打印矩阵时必须说明形状、行含义、列含义，并至少展开一个具体行。
- 首次实验优先使用最小数字和可手算例子，不提前引入无关复杂度。
- 每节只解决一个主要认知跳转，其他概念放入后续课程。
- 教材必须区分稳定原理、为教学做的简化和真实模型中的实现。
- 用户尚未运行实验前，不直接进入结果追问。
- 学习者默认阅读每课的 `OUTPUT.md`，不要求亲自执行脚本。
- 修改课程 README、`lesson.py`、`lab.py` 或运行脚本后，必须运行
  `python3 scripts/render_lesson_outputs.py`，把真实脚本输出更新到对应课程的
  `OUTPUT.md`。
- `OUTPUT.md` 是自动生成的阅读材料，不手工修改；需要改善讲解时修改源脚本后重新生成。
- 教程脚本的标准输出必须是合法 Markdown 正文：步骤使用二级标题，步骤内的分支使用
  三级标题，矩阵和逐行公式使用代码块，对照结果优先使用表格。不得把整篇输出包在
  一个代码块中。
- 当一课包含多个依赖步骤或容易混淆的对象层次时，在 `OUTPUT.md` 顶部提供 Mermaid
  知识流程图；图中必须同时表达处理顺序、关键 Shape 和对象之间的包含关系。
- 在知识流程图之前，必须先说明本课机制的目的、位于模型哪一层计算、训练和推理是否
  都会使用，以及本课明确不包含哪些训练或推理专属步骤。

## 验证要求

修改 Python 教材脚本后至少运行：

```bash
python3 -m py_compile <modified-files>
python3 scripts/render_lesson_outputs.py
```

检查生成的 `OUTPUT.md` 是否为中文、是否包含课程位置和本节结论，并确认其中内容
来自脚本的真实运行结果。

## 文件所有权

- `README.md`：仓库入口和当前学习方式；
- `docs/learning-roadmap.md`：整体课程顺序与边界；
- `lessons/**/README.md`：单节目标、结构导航、示例约定和参考资料；
- `lessons/**/lesson.py`：可直接运行的中文教程脚本；
- `lessons/**/lab.py`：可选的逐段实验文件。
- `lessons/**/OUTPUT.md`：自动生成、供学习者直接阅读的完整教程输出；
- `scripts/render_lesson_outputs.py`：运行所有课程并刷新 `OUTPUT.md`。
